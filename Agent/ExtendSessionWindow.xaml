<Window x:Class="ControlLaboratorio.Agent.ExtendSessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aviso de Sesión" Height="180" Width="420"
        WindowStyle="None" Topmost="True" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Border Background="#1E293B" CornerRadius="14" BorderBrush="#FACC15" BorderThickness="2" Padding="20,15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Opacity="0.6" Direction="270" ShadowDepth="4"/>
        </Border.Effect>
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="⏰ Tu sesión está por terminar" Foreground="#FACC15" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="¿Deseas ampliar tu tiempo 3 horas más?" Foreground="White" FontSize="13" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="BtnYes" Content="  Sí, ampliar  " Click="BtnYes_Click" Background="#22C55E" Foreground="White" FontWeight="Bold" FontSize="13" BorderThickness="0" Padding="20,10" Cursor="Hand" Margin="0,0,12,0">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                </Button>
                <Button x:Name="BtnNo" Content="  No, gracias  " Click="BtnNo_Click" Background="#64748B" Foreground="White" FontWeight="Bold" FontSize="13" BorderThickness="0" Padding="20,10" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
